/**
 * Test file for MCP server
 */

function greet(name) {
  return `Hello, ${name}!`;
}

// Some potential issues to test code review:
// 1. No parameter validation
// 2. No documentation
// 3. No error handling

module.exports = {
  greet
};
