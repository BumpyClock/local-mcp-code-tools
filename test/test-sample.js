/**
 * Test sample file for MCP testing
 */

function add(a, b) {
  // Add two numbers
  return a + b;
}

function subtract(a, b) {
  // Subtract b from a
  return a - b;
}

// Export the functions
module.exports = {
  add,
  subtract
};
